export const radius = {
  xs: 6,
  sm: 8,
  md: 12,
  /** Input fields, small buttons, pill chips. */
  input: 14,
  lg: 16,
  /** Cards, section groups. */
  card: 20,
  xl: 20,
  /** Bottom sheet / modal top corners. Canonical sheet family. */
  sheet: 28,
  glass: 20,
  full: 9999,
} as const;
