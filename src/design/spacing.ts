export const spacing = {
  xxs: 2,
  xs: 4,
  sm: 8,
  /** Finer step between sm and md — section headers, tight form groups. */
  base: 12,
  md: 16,
  /** Finer step between md and lg — card padding, hero gaps. */
  comfort: 20,
  lg: 24,
  xl: 32,
  xxl: 48,
} as const;
