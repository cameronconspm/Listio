import React from 'react';
import { SettingsStack } from './SettingsStack';

/** Bottom tab: former Settings hub and all settings routes. */
export function ProfileStack() {
  return <SettingsStack hubPresentation="tab" />;
}
