/**
 * @deprecated Import from `src/ui/chrome` — kept for existing navigators.
 * Shared frosted tab bar chrome (real blur + tint).
 */
export { TabBarChromeBackground as TabBarBlurBackground } from '../ui/chrome/TabBarChromeBackground';
